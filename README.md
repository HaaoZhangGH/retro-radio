# Retro Radio

本项目是一个“复古收音机”网页小玩具：点击 POWER 开机，点击旋钮扫台；使用 Web Audio 的真实频谱驱动屏幕均衡器与扬声器点阵律动；支持导入/导出电台 JSON、拖入本地音频；并可作为 PWA 离线使用。

## 开发
```bash
npm install
npm run dev
```

## 构建 / 预览（用于测试离线 & PWA）
```bash
npm run build
npm run preview
```

规划文档见 `docs/PLAN.md`。

